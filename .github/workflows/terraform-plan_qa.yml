name: "Terraform Plan Develop - QA"

on:
  pull_request:
    branches:
      - develop

env:
  TF_CLOUD_ORGANIZATION: "lblasnar"
  TF_API_TOKEN: "${{ secrets.TF_API_TOKEN }}"
  TF_WORKSPACE: "aws-lambda-lab-gh-actions"
  CONFIG_DIRECTORY: "./deploy"

jobs:
  terraform:
    if: github.repository != 'hashicorp-education/learn-terraform-github-actions'
    name: "Terraform Plan QA"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Cache SonarCloud packages
        uses: actions/cache@v3
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar
      - name: Cache Maven packages
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2
      - name: Build with Maven
        run: mvn --batch-mode --update-snapshots verify
      - name: Verify jar created
        run: |
          FILE=target/LambdaTest-1.0-SNAPSHOT.jar
          if test -f "$FILE"; 
          then
            echo "$FILE exists."
            cp $FILE deploy
          else
            echo "$FILE not exists."
            EXIT /B 2
          fi
      - name: Build and analyze
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed to get PR information, if any
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=lblasnar_awslamda
      - name: Upload Configuration
        uses: hashicorp/tfc-workflows-github/actions/upload-configuration@v1.0.0
        id: plan-upload
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          directory: ${{ env.CONFIG_DIRECTORY }}
          speculative: true
      - name: Create Plan Run
        uses: hashicorp/tfc-workflows-github/actions/create-run@v1.0.0
        id: plan-run
        with:
          workspace: ${{ env.TF_WORKSPACE }}
          configuration_version: ${{ steps.plan-upload.outputs.configuration_version_id }}
          plan_only: true
        env:
          #  https://github.com/hashicorp/tfc-workflows-github/issues/10
          TF_VAR_environment: "\"QA\""
          TF_VAR_sqs_name: "\"SQS_QA\""
          TF_VAR_sqs_delay: 90
          TF_VAR_sqs_max_message_size: 2048
          TF_VAR_sqs_message_retention_seconds: 86400
          TF_VAR_sqs_receive_wait_time_seconds: 10
          TF_VAR_lambda_runtime: "\"java11\""
          TF_VAR_lambda_handler: "\"org.example.lambda.HandlerStream\""
          TF_VAR_lambda_function_name: "\"Lab_lambda_function_QA\""
          TF_VAR_lambda_filename: "\"LambdaTest-1.0-SNAPSHOT.jar\""
          TF_VAR_lambda_memory_size: 1024
          TF_VAR_lambda_timeout: 20
          TF_VAR_lambda_publish: true
          TF_VAR_lambda_alias_name: "\"QA\""
          TF_VAR_lambda_alias_description: "\"QA Environment\""
          TF_VAR_lambda_alias_version: 9
          TF_VAR_lambda_event_source_mapping_enabled: true
          TF_VAR_lambda_allow_invocation_from_sns_statement_id: "\"AllowExecutionFromSNSPR\""
          TF_VAR_lambda_allow_invocation_from_sns_action: "\"lambda:*\""
          TF_VAR_lambda_allow_invocation_from_sns_principal: "\"sns.amazonaws.com\""
          TF_VAR_lambda_role_name: "\"LambdaRole_QA\""
          TF_VAR_sns_protocol: "\"lambda\""
          TF_VAR_sns_name: "\"SNS_QA\""

      - name: Get Plan Output
        uses: hashicorp/tfc-workflows-github/actions/plan-output@v1.0.0
        id: plan-output
        with:
          plan: ${{ fromJSON(steps.plan-run.outputs.payload).data.relationships.plan.data.id }}

      - name: Update PR
        uses: actions/github-script@v6
        id: plan-comment
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            // 1. Retrieve existing bot comments for the PR
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const botComment = comments.find(comment => {
              return comment.user.type === 'Bot' && comment.body.includes('Terraform Cloud Plan Output')
            });
            const output = `#### Terraform Cloud Plan Output
               \`\`\`
               Plan: ${{ steps.plan-output.outputs.add }} to add, ${{ steps.plan-output.outputs.change }} to change, ${{ steps.plan-output.outputs.destroy }} to destroy.
               \`\`\`
               [Terraform Cloud Plan](${{ steps.plan-run.outputs.run_link }})
               `;
            // 3. Delete previous comment so PR timeline makes sense
            if (botComment) {
              github.rest.issues.deleteComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
              });
            }
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            });

  test_lambda:
    runs-on: ubuntu-latest
    name: "Test lambda once deploy"
    needs: [ terraform ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: Configure AWS Region
        run: |
          aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws configure set region us-east-1
      - name: Test Lambda QA
        run: |
          echo -n '{"EventSource": "aws:sns","EventVersion": "1.0","EventSubscriptionArn": "arn:aws:sns:us-east-1:350407421116:SNS_QA:aafccea1-72c7-4399-b18e-541b42c47eed","Sns": {"Type ": "Notification ","MessageId": "f8a0434d-7f4f-57f6-8d1c-1e6216f9bf3b ","TopicArn ": "arn: aws: sns: us - east - 1: 350407421116: SNS_QA ","Subject ": "purchase - transaction - subject ","Message ": {"id": "2662585","description": "Test Payload"},"Timestamp ": "2023-07-17T16:08:22.624Z","SignatureVersion ": "1 ","Signature ": "puuziWgy5icQuG01oX / pUDvI44VNt9A + FMzzDUYAOl88jGkUXtQEoMemb + oZumAWAZ2OoZiwHboN54HuLdi + XwpvQhYp7NL7jbuAws / CY9VWgbn6wRw8pLaUyryxBKnoxqgt + b7kSW9bXG9o466R + mBw4A8j2Tokng3fZKoikUVHiTCCM2ke7P6W1SGuFANzyk6K2RBpGlQpObWju0YhiHeRS2e51gDLXP5bgRkp07GkF5MSj1sUMWitbl1pcWv / UKPTdI5VsNgsnJviIzYnajvf1QHAgrG5N6MaDmSjutj7 / Q2c4AkWF2EvGkNunu6RYUIVwpuFdSjMRAYO3TME7A == ","SigningCertUrl ": "https: //sns.us-east-1.amazonaws.com/SimpleNotificationService-01d088a6f77103d0fe307c0069e40ed6.pem","UnsubscribeUrl": "https://sns.us-east-1.amazonaws.com/?Action=Unsubscribe&SubscriptionArn=arn:aws:sns:us-east-1:350407421116:SNS_QA:aafccea1-72c7-4399-b18e-541b42c47eed","MessageAttributes": {"NOTIFICATION_SUBJECT_HEADER": {"Type": "String","Value": "purchase-transaction-subject"},"id": {"Type": "String","Value": "a26d9c4c-6d48-bc50-6363-42e288f05f4a"},"contentType": {"Type ": "String ","Value ": "application / json "},"timestamp": {"Type ": "String ","Value ": "1689610101979 "}}}}' | base64 > payload.json
          aws lambda invoke --function-name Lab_lambda_function_QA --payload file://payload.json output.json
          cat output.json
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_EC2_METADATA_DISABLED: true
      - name: Check if running correctly
        run: |
          if jq -e '. | .StatusCode == 200' output.json >/dev/null; then
            echo "Lambda running correctly."
          else  
          echo "Lambda not running correctly."
            # Revert con el hash del ultimo commit
            # git revert $GITHUB_SHA" --no-edit
            # git push origin HEAD
            # git checkout develop
            # git pull origin develop
            # git push origin develop
            # hub pull-request -b develop -m "Revert changes introduced by the pull request"
            # EXIT /B 2
          fi
